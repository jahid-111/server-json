{
  "scripts": {
    "build": "react-scripts build",
    "start": "react-scripts start",
    "server": "json-server data/db.json --port 8000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
} 